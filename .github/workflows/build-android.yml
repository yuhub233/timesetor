name: Build Android APK

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        cache: true
    
    - name: Prepare Flutter project
      working-directory: android
      run: |
        cp pubspec.yaml ../pubspec.yaml.bak
        cp -r lib ../lib.bak
        cp -r android/app/src/main ../android_main_bak
        cp android/app/build.gradle ../app_build.gradle.bak
        cp android/build.gradle ../root_build.gradle.bak
        cp android/gradle.properties ../gradle.properties.bak
        cd ..
        rm -rf android
        flutter create --org com.timesetor --project-name timesetor android
        cd android
        cp ../pubspec.yaml.bak pubspec.yaml
        rm -rf lib
        cp -r ../lib.bak lib
        flutter pub get
    
    - name: Apply custom Android configuration
      working-directory: android
      run: |
        cp -r ../android_main_bak/* android/app/src/main/
        cp ../app_build.gradle.bak android/app/build.gradle
        cp ../root_build.gradle.bak android/build.gradle
        cp ../gradle.properties.bak android/gradle.properties
    
    - name: Build APK
      working-directory: android
      run: flutter build apk --release
    
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: timesetor-apk
        path: android/build/app/outputs/flutter-apk/app-release.apk
        retention-days: 30
  
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Download APK artifact
      uses: actions/download-artifact@v4
      with:
        name: timesetor-apk
        path: ./apk
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: ./apk/app-release.apk
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
